import { Metadata } from "next"
export const metadata: Metadata = {
    title: "Admin Register"
}
export default function Register() {
    return <h1 className="bg-black font-bold">Register Page</h1>
}